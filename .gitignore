# Rust (every tool except ccx)
target/

# Node — no tool here has Node packages any more (pocket only runs qmd through npx);
# kept so a stray node_modules is never committed
node_modules/
*.tsbuildinfo

# Claude local overrides — machine-specific, keep shared settings.json tracked
**/.claude/settings.local.json

# Secrets — nothing here should ever be committed
.env
.env.*
*.pem
*.p12
*.key
.netrc

# Editors
.idea/
.vscode/
*.swp
*.swo
*~

# macOS
.DS_Store
